# Ignore everything by default
/*

# Allow this .gitignore itself
!/.gitignore

# Allow the landing page
!/index.html

# Allow the dashboard folder, but re-ignore its contents by default…
!/master-plan/
# …then explicitly allow only the dashboard entry file
/master-plan/*
!/master-plan/index.html
